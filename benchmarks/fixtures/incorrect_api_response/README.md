# Incorrect API response fixture

This intentionally failing miniature repository backs the Phase 1 sample task.
Its test is independent of the task description and fails until `app.py` returns
the documented response. The mock solution is injected by the demo, not stored
in the benchmark manifest.

